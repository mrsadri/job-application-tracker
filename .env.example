# Frontend Configuration
# Set this to your deployed crawler API URL
CRAWLER_API_URL=http://localhost:3000/api/crawl

# GitHub Pages base URL (if deploying to GitHub Pages subdirectory)
# Leave empty if deploying to root domain
PUBLIC_URL=
